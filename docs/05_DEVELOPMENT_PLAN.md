# 05. Development Plan

1. Bootstrap the local Python project and documentation.
2. Add SQLite job persistence and a FIFO queue.
3. Add one local worker and a process lock for GPU stages.
4. Adapt the required ComfyUI client and workflow patching code.
5. Add audio input and one TTS option.
6. Add one compatible talking-avatar or lip-sync runtime.
7. Add FFmpeg postprocessing, manifests and previews.
8. Add manual operator review and accepted or rejected states.
9. Add a small localhost interface only after the command-line flow works.

Each milestone ends with tests, documentation, artifacts, commit, push and clean Git.
